{
  "name": "@drone-drive/frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "preview": "vite preview",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "ol": "^10.3.0",
    "@drone-drive/contracts": "0.1.0"
  },
  "devDependencies": {
    "vite": "^8.2.1"
  }
}
